# Base de todas as rotas. Sempre termina em /v1, sem barra final.
VITE_API_BASE_URL=http://localhost:8000/v1

# Modo demo: servidor falso em memória por trás do mesmo transporte.
# Espelha o `--dart-define-from-file=env/demo.json` do app Flutter.
VITE_DEMO_MODE=false

# ── Supabase (branch feat/react-supabase) ────────────────────────────
# Dashboard > Settings > API. Só a chave "anon"/"public" — nunca a service_role.
VITE_SUPABASE_URL=https://xxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
