[pytest]
# `tests_e2e/test_fluxo_completo.py` é um script manual contra o Supabase real,
# não uma suíte pytest. Mantê-lo fora da descoberta impede que `pytest` tente
# executá-lo sem a API local e sem as credenciais de rede necessárias.
testpaths = tests
